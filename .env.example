DATABASE_URL=postgres://twenty:twenty@localhost:5432/twenty

# Stripe connector credentials
# Use a restricted key (sk_...) scoped to Checkout Sessions / Customers for production tenants.
STRIPE_API_KEY=

# Webhook signing secret for the endpoint pointing to /api/fundraising/webhooks/stripe
STRIPE_WEBHOOK_SECRET=
